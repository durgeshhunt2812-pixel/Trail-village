# Backend

This folder contains the Flask app and runtime data.

Run it from here:

```powershell
cd backend
python app.py
```

The app stores SQLite databases in `backend/data/`, keeps a backend-local static directory in `backend/static/`, and serves API routes plus the HTML templates from the frontend folder.
